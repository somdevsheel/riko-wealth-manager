# Override the backend base URL (needed for Expo Go on a physical device —
# use your machine's LAN IP, e.g. http://192.168.1.23:8000).
# Defaults: http://localhost:8000 (web/iOS), http://10.0.2.2:8000 (Android emulator).
EXPO_PUBLIC_API_URL=
